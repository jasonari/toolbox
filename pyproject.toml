[project]
name = "toolbox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyperclip>=1.11.0",
    "rich>=15.0.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.22.2",
]
